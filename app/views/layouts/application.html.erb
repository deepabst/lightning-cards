<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Lightning Cards</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>
  <body>
    <nav>
      <%= image_tag 'LC_Logo.svg', alt:'Lightning Cards Logo' %>
    </nav>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <h2 class="navbar-text">Lightning Cards</h2>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
        <% if @current_user.present? %>
          <a class="navbar-brand" href="#">Welcome back, <span class="username"><%= @current_user.name %></span></a>
          <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
            <li class="nav-item">
              <%= link_to 'Home', root_path, :class => "nav-link"  %>
            </li>
            <li class="nav-item">
              <%= link_to 'Logout', login_path, method: 'delete', :class => "nav-link"  %>
            </li>
            <li class="nav-item">
              <%= link_to 'Build A Deck', new_deck_path, :class => "nav-link"   %>
            </li>
            <li class="nav-item">
              <%= link_to 'My Decks', decks_path, :class => "nav-link"  %>
            </li>
          <% else %>
            <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
              <li class="nav-item">
                <%= link_to 'Login', login_path, :class => "nav-link"  %>
              </li>
              <li class="nav-item">
                <%= link_to 'Sign Up', new_user_path, :class => "nav-link"  %>
              </li>
            <% end %>
          </ul>
        </div>
      </nav>
      <div class="container">
        <%= yield %>
      </div>
      <%# <button id="navtoggle" onclick="toggleNavBar()">Zen Mode</button> %>
      <footer class="footer mt-auto bg-dark text-white">
        Dee Pabst 2022
      </footer>
    </body>
  </html>
