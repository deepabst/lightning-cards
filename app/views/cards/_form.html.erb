<%= bootstrap_form_with model: @card, local: true do |f| %>
  <%= f.collection_select :deck_id, Deck.where(user_id: @current_user.id), :id, :name, include_blank: true %>
  <%= f.text_area :front_text %>
  <%= f.text_area :back_text %>
  <%= f.submit %>
<% end %>
